model FundPool {
  id       Int    @id @default(autoincrement())
  grantId  Int
  grant    Grant  @relation(fields: [grantId], references: [id])
  amount   Decimal  @db.Decimal(10, 2)
  category String
  expenses Expense[]
}

enum fundPoolCategory {
    RENT
    FURNITURE
    GENERAL
}