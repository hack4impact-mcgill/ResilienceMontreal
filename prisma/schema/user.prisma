model User {
  name        String
  email       String   @unique
  isConfirmed Boolean  @default(false)
  supabaseId  String   @id
  role        RoleName @default(Unassigned)
  clients     Client[]
}
