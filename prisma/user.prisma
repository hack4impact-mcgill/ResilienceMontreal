model User {
    id    Int     @id @default(autoincrement())
    name  String
    email String  @unique
    password String
    role  Role   
    refresh_token String
    access_token String
}

enum Role {
    RESPONDER
    BOOKKEEPER
    ADMIN
}